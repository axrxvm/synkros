const maxAllowedFileSize = 500 * 1024 * 1024;
const maxAllowedFileSizeInWords = "500MB";
const cleanupTimeLimit = 24 * 60 * 60 * 1000;

module.exports = {
  maxAllowedFileSize,
  maxAllowedFileSizeInWords,
  cleanupTimeLimit,
};
