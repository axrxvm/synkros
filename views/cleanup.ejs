<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Synkros - File Cleanup</title>

    <link rel="apple-touch-icon" href="/apple-icon.png" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="stylesheet" href="/css/style.min.css" />
  </head>

  <body>
    <div class="block-message">
      <h4>Clean Up Done!</h4>
      <p style="text-align: center">
        <%=locals.message %> <% if(locals.totalFiles == 0) { %>
        <br /><br /><small style="opacity: 0.6"
          >Only files uploaded more that 24 hours ago can be cleaned. The files uploaded are auto-deleted after 24 hours.</small
        >
        <% } %>
      </p>
  <button class="js-goto-home">Go to Home</button>
    </div>

    <script nonce="<%= locals.cspNonce %>">
      document.addEventListener('DOMContentLoaded', function() {
        const els = document.querySelectorAll('.js-goto-home');
        els.forEach(el => el.addEventListener('click', () => { window.location.href = '/'; }));
      });
    </script>
    <script src="/js/security.js"></script>
  </body>
</html>
